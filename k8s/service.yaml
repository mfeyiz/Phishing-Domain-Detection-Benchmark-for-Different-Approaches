apiVersion: v1
kind: Service
metadata:
  name: phishing-detector
  labels:
    app: phishing-detector
spec:
  type: NodePort
  ports:
  - port: 8000
    targetPort: 8000
    nodePort: 30080
    protocol: TCP
    name: http
  selector:
    app: phishing-detector
